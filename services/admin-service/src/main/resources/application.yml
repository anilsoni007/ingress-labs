server:
  port: 8090

spring:
  application:
    name: admin-service
  security:
    user:
      name: admin
      password: admin123
      roles: ADMIN

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.ingresslab.admin: DEBUG
    org.springframework.security: DEBUG